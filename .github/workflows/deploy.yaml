name: Package Deployment

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    uses: versioning-cli/versioning-app/.github/workflows/analyze.yaml@main
    timeout-minutes: 5

    steps:
      - name: Build
        run: dart compile exe bin/app.dart -o versioning.exe
